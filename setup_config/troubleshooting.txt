Cuando instalamos el chart fake-gpu-operator:

kubectl label node <node-name> run.ai/simulated-gpu-node-pool=default

helm upgrade -i gpu-operator oci://ghcr.io/run-ai/fake-gpu-operator/fake-gpu-operator -f fake-gpu-operator-values.yaml --namespace gpu-operator --create-namespace

kubectl edit clusterrole fake-status-updater #Y añadimos "- update" debajo en la sección de nodo
Esto se podría hacer son sed en el script

kubectl delete configmaps -n gpu-operator topology-node2 topology-node3 

kubectl delete pods -n gpu-operator device-plugin-* device-plugin-* status-updater-*


Proceso de upgrade:

helm upgrade gpu-operator oci://ghcr.io/run-ai/fake-gpu-operator/fake-gpu-operator -f fake-gpu-operator-values.yaml --namespace gpu-operator

kubectl delete configmaps -n gpu-operator topology-node2 topology-node3 

kubectl delete pods -n gpu-operator device-plugin-* device-plugin-* status-updater-*