ssh -A JoelGJ@c220g5-120130.wisc.cloudlab.us

tmux new -s kubespray

sudo git clone https://repo.fib.upc.es/joel.gonzalez.jimenez/cloudlab-k8s.git

=============tmux guide=============

tmux new -s kubespray

ctrl + b + d (deattach)

ctrl + d (destroy)

Ctrl+b + [ (modo vista para poder scrollear)
para salir clicar "q"

=============fin guide=============

ansible-playbook -i inventory/mycluster/inventory.ini reset.yml -b -v --limit node2,node3

eval "$(ssh-agent -s)"