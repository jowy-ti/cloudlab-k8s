ssh -A JoelGJ@c220g5-120130.wisc.cloudlab.us

tmux new -s kubespray

sudo git clone https://repo.fib.upc.es/joel.gonzalez.jimenez/cloudlab-k8s.git

cd /opt/cloudlab-k8s/setup_config

source ./setup.sh (en caso de no usar source, hacer al acabar el script source ~/.bashrc)

./scale.sh -n num_workers

=============tmux guide=============

tmux new -s kubespray

ctrl + b + d (deattach)

ctrl + d (destroy)

Ctrl+b + [ (modo vista para poder scrollear)
para salir clicar "q"




sudo ip link set dev enp1s0 mtu 1450